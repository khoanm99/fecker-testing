const Input = () => {
  const defaultCls = '';
  return (
    <>
      <input
        className={` w-full rounded-full border border-black bg-transparent py-3 text-[14px]`}
      />
    </>
  );
};

export default Input;
