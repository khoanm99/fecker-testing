import { memo } from 'react';

const AppFooter = () => {
  return (
    <footer id="footer" className="w-full relative z-30 text-primary">
      Footer
    </footer>
  );
};

export default memo(AppFooter);
